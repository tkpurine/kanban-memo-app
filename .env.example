# Set a password to enable authentication (leave empty to disable)
APP_PASSWORD=

# Server port (default: 3000)
PORT=3000

# Storage folder (set to enable cloud mode, leave empty for local mode)
# In cloud mode, the folder modal is skipped and this path is used automatically.
# Example for Render: /data/kanban
STORAGE_FOLDER=
